// Optional enhancement: convert Header into a fully responsive nav that collapses under 900px.
// For simplicity, Header already inlines a mobile toggle.
export default function MobileNav() { return null; }